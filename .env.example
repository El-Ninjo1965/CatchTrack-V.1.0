# Local bootstrap configuration for development previews.
# Copy to .env and set local values before running tests.
CORE_BOOTSTRAP_PASSWORD=change-this-local-password
CORE_BOOTSTRAP_USERNAME=developer
