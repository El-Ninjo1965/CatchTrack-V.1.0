# Local bootstrap configuration for developer login testing only.
# Copy to .env and set a local non-shared value before starting the app.
CORE_BOOTSTRAP_PASSWORD=change-this-local-password
CORE_BOOTSTRAP_USERNAME=developer
